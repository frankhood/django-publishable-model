============
Installation
============

At the command line::

    $ easy_install django-publishable-model

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publishable-model
    $ pip install django-publishable-model
